defaults:
  - base_dataset
  - view_sampler: bounded

name: replica_semseg
roots: [/workspace/data/replica_semseg]
scenes: [office0]
make_baseline_1: true
augment: true

input_image_shape: [252, 252]
original_image_shape: [680, 1200]
cameras_are_circular: false

baseline_min: 1e-3
baseline_max: 1e2
max_fov: 120.0

skip_bad_shape: false
num_of_inputs: 2
prompt_strategy: centroid
min_object_pixels: 16
